
now do it in python..

start by curling the install script

check for git
    if no then install
check for brew
    if no then install

check for python
    if no then install

check for virtualenv    
    if no then install

Then clone the repository

Then run setup.py within the repository

    create a virtualenv title sphinx-build in the directory
    activate the environmental
    install requirements

Run a python based build script next
    same as the bash script that i already wrote.  The only difference is that it will be written in python... or possibly go just for fun...
    


install script will clone the repository 

install brew




Start with installing brew 

then install python


then run a setup.py command that sets the 

environmental variables