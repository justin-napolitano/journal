# Pullit.sh

:::{topic} Introduction
Pullit is a script that pulls from github and builds a sphinx project.  

It then does a commit -a and pushes the new build to the remote repository.  
:::

:::{literalinclude} code-base/pullit.sh
:language: Bash

:::
